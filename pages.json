{
  "routes": [
  
    { "src": "/api/(.*)", "dest": "https://haya-api.YOUR_WORKERS_SUBDOMAIN.workers.dev/api/$1" },
   
    { "handle": "filesystem" },
   
    { "src": "/(.*)",     "dest": "/$1" }
  ]
}
